apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: true

  - name: PostgreSQL
    type: postgres
    access: proxy
    url: postgres:5432
    user: mirai
    secureJsonData:
      password: mirai123
    jsonData:
      database: mirai_db
      sslmode: disable
    editable: true