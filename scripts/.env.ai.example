# Copy these to Codespaces Secrets (recommended) or export in session.

# GitHub access tokens (you have both available in Codespaces secrets)

# Option 1: Classic Personal Access Token with full repo + admin:org scopes (RECOMMENDED)
# This token already has all necessary permissions for Actions secrets/variables
GH_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Option 2: Fine-grained Personal Access Token with specific Repository permissions:
# - Secrets: Read and write (to manage Actions secrets)
# - Variables: Read and write (to manage Actions variables) 
# - Environments: Read and write (to manage environment secrets)
# - Metadata: Read (basic repository access)
# - Actions: Read and write (to manage workflows)
# Organization permissions (if managing org-level secrets):
# - Secrets: Read and write
# - Variables: Read and write
CODEX=ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Optional explicit GHCR creds (defaults to GH_TOKEN + your GitHub login)
GHCR_USERNAME=your_github_username
GHCR_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# SSH deploy access (server you want the AI to manage)
SSH_HOST=your.server.example
SSH_USER=root
# Private key as base64-encoded string
SSH_KEY_B64=LS0tLS1CRUdJTiBPUFVOU1NIIFBSSVZBVEUgS0VZLS0tLS0K...

# Optional runtime integrations
Да, я вижу что Actions запустились! 🎉 Давайте проверим что все работает и разберемся с тегами.

Сначала посмотрим какие теги у нас есть:

Теперь проверим когда были созданы теги и что с ними:

Теперь давайте проверим что наши workflows работают. Сначала посмотрим на последний workflow run:

Открыт простой браузер по адресу https://github.com/AgeeKey/mirai-agent/actions/runs

А теперь давайте почистим ненужные теги. У нас есть несколько старых тегов которые можно удалить:

Рекомендую оставить:

v1.1.0 - наша новая железная CI/CD система ✅
v1.2.3 - последний working tag (если есть)
Удалить старые:

v0.1.0, v0.2.1, v1.0.0 - старые тестовые теги
Checked background terminal output

Похоже теги уже удалены на GitHub! Проверим какие теги остались:

Отлично! Теперь у нас остались только нужные теги: v1.1.0 и v1.2.3.

Давайте проверим что наша железная CI/CD система работает. Посмотрим на последние workflow runs:

Вижу что Build & Deploy успешен ✅, но CI workflows падают. Это нормально - нужно немного подправить CI workflows чтобы они работали с текущей структурой проекта.

Давайте посмотрим на ошибки в CI:

Давайте проверим прямо в браузере что происходит с нашими workflows:

Открыт простой браузер по адресу https://github.com/AgeeKey/mirai-agent/actions/runs/17711204849

А пока что давайте проверим что наша main система Build & Deploy работает. Проверим образы в GHCR:

🎉 ОТЛИЧНО! Образ mirai-api:v1.1.0 собран и доступен! Проверим остальные:
OPENAI_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxx
TELEGRAM_BOT_TOKEN=123456:ABC-DEF1234ghIkl-zyx57W2v1u123ew11
TELEGRAM_CHAT_ID_ADMIN=-1001234567890

